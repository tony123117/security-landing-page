# Lion Guard Security — React App

Quick scaffold converting the provided `home.html` into a React app using Vite.

Install and run:

```bash
npm install
npm run dev
```

Open http://localhost:5173 (or the port shown by Vite).

Files added:
- index.html
- src/main.jsx
- src/App.jsx
- package.json
